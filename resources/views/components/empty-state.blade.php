@props(['message' => 'No tasks for today! Add one above.'])

<div class="text-center py-8 text-gray-500 dark:text-gray-400">
    <i class="far fa-smile-beam text-4xl mb-2"></i>
    <p class="text-lg">{{ $message }}</p>
</div>

